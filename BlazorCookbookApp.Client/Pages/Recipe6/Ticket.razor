<h3>Ticket component</h3>

<p>Ticket component uses parameters it gets from SellingTickets component page.</p>
<p>It has a button that calls the OnAdded event.</p>

<div class="ticket">
    <div class="name">@Tariff</div>
    <div class="price">@Price.ToString("0.00 $")</div>
    <div class="ticket-actions">
        <button @onclick="@Add">Add to cart</button>
    </div>
</div>

@code {
    [CascadingParameter]
    public Cart? Cart { get; set; }

    public void Add() => Cart?.Add(Tariff, Price);

    [Parameter, EditorRequired]
    public string Tariff { get; set; } = default!;

    [Parameter, EditorRequired]
    public decimal Price { get; set; }

    [Parameter] // Optional
    public EventCallback OnAdded { get; set; }
}
